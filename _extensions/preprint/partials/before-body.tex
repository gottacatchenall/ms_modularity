$-- Implements the frontmatter, title page, and abstract.
$--
$-- %%%% TODO %%%%%
$-- Customize is needed, otherwise remove this partials to use Quarto default one
$-- %%%%%%%%%%%%%%%%

%\setsansfont{Roboto}
%\setmathfont{TeX Gyre Termes Math}[version=termes]
\setstretch{1.15}

$if(formatting.draft-format)$\thispagestyle{empty}$endif$

\parbox{14cm}{\setstretch{0.9}\raggedright\bfseries\huge\nohyphens{${title}}}

$if(formatting.masked-review)$
$else$
$if(formatting.draft-format)$\vfil$else$\vskip 1em$endif$
\normalsize ${ by-author:_authors.tex()[, ] }

$endif$
$if(formatting.draft-format)$\vfil$else$\vskip 2em$endif$
{\normalsize\centering
$if(abstract)$
\textbf{Abstract} $abstract$
$endif$
$if(formatting.draft-format)$\vfil$else$\vskip 1em$endif$
$if(keywords)$
\textbf{Keywords:} %
$for(keywords/allbutlast)$${it}, $endfor$%
$for(keywords/last)$${it}$endfor$%
$endif$
}
$if(formatting.draft-format)$\clearpage$else$\vskip 4em$endif$
$if(formatting.draft-format)$
\setcounter{page}{1}
\doublespacing
\linenumbers
$else$
%\flushleft
\raggedright
$endif$